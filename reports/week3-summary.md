# Week 3 Summary

**Focus:** Bash scripting fundamentals and applied automation for system checks.  
**Duration:** Monday – Thursday  
**Progress:** Completed through Ping Sweep (Day 4)  

---

## What I Worked On
- **greet.sh** – created a script that greets users by name using variables and `echo`. Practiced reading user input and formatting terminal output.
- **ping-sweep.sh** – developed a loop-based script that pings multiple websites and confirms which are reachable or offline. Practiced using redirection (`>/dev/null 2>&1`) and conditional logic.
- **file-check.sh** – used `if/else` statements with the `-f` flag to verify whether a file exists in a directory, reinforcing test expressions and condition handling in Bash.
- Continued self-paced learning in the **ChatGPT for SOC Analysts** course — completed Chapter 2, focusing on applying AI to log analysis and SOC workflows.

---

## Key Takeaways
1. Reinforced Bash syntax: variables, loops, conditionals, and command substitution.
2. Learned the difference between logical operators (`&&`, `||`) and file test operators (`-f`, `-d`, etc.).
3. Gained confidence in writing modular, reusable scripts that perform simple network and file-system checks — skills directly relevant to SOC automation tasks.

---

## Next Steps
1. Explore local network scanning using internal IP ranges.
2. Add logging and timestamp functionality to Bash scripts.
3. Begin documenting scripts with Markdown `notes.md` files for each lab (to upload to GitHub).
